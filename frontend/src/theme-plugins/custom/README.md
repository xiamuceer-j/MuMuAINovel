# Custom Themes

Place your custom theme folders here.

Each theme folder should contain:
- `manifest.json` - Theme metadata
- `index.ts` - Theme configuration

Example structure:
```
custom/
  my-theme/
    manifest.json
    index.ts
```

See `../built-in/default/` for an example.
